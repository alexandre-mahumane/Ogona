/** @deprecated Prefer `@/hooks/useAuth` — mantido para imports existentes. */
export {
  useForgotPasswordMutation,
  useLoginMutation,
  useLogoutMutation,
  useMeQuery,
  useRegisterMutation,
  useResetPasswordMutation,
  useSendOtpMutation,
  useSendRegisterOtpMutation,
  useUpdateProfileMutation,
  useVerifyOtpMutation,
  useVerifyRegisterOtpMutation,
} from '@/hooks/useAuth';
